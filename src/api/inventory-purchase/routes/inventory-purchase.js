'use strict';

/**
 * inventory-purchase router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::inventory-purchase.inventory-purchase');
