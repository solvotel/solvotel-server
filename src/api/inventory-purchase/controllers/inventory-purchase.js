'use strict';

/**
 * inventory-purchase controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::inventory-purchase.inventory-purchase');
